.venv
.env
.git
.github
__pycache__
**/__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
tests
docs
CHANGELOG.md
# README.md NAO entra aqui: o pyproject.toml o declara em `readme = "README.md"`,
# entao o `pip install .` do Dockerfile precisa dele no contexto de build.
.gitignore
.env.example
.editorconfig
.pre-commit-config.yaml

# INF-3: o `.dockerignore` antigo ignorava `DataBase.db` mas NAO a pasta
# `data/` -- entao o `COPY . .` assava o banco local, com o historico real de
# coletas, dentro de cada imagem.
data/
*.db
*.db-wal
*.db-shm
*.log
